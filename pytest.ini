[pytest]
# Sammle Tests standardmäßig nur aus dem tests/-Verzeichnis.
# Das verhindert, dass pytest über Wegwerf-Skripte im gitignored scratch/-Ordner
# stolpert (z.B. scratch/test_search_api.py).
testpaths = tests
norecursedirs = scratch .git graphify-out *.app
